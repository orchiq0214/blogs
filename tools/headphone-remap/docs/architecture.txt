┌─────────────────────────┐
                       │  3.5mm 耳机 (CTIA)      │
                       │  [+ ] [−] [中键]        │
                       └──────────┬──────────────┘
                                  │ 物理按键 (100Ω 短路)
                                  ▼
                       ┌─────────────────────────┐
                       │  Realtek HDAudio Codec  │
                       │  检测 + 注入 Windows 键  │
                       └──────────┬──────────────┘
                                  │ vkB3/AF/AE, sc=0
                                  ▼
                ┌────────────────────────────────────┐
                │   Windows OS 键盘事件层            │
                │   (PnP AudioEndpoint 注册/反注册)  │
                └───┬──────────────────────────┬─────┘
                    │ 插/拔                  │ 按键事件
                    ▼                        ▼
         ┌────────────────────┐    ┌─────────────────────┐
         │ headphone_watcher  │    │  AHK low-level hook │
         │ (PowerShell 轮询)  │    │  (Media_Play_Pause  │
         │ 每 2s 检查 PnP     │    │   占位 + vkB3)      │
         └─────┬──────────────┘    └──────────┬──────────┘
               │ 状态变化                       │ 命中
               ▼                                ▼
         ┌────────────────┐           ┌──────────────────┐
         │ Start/Stop     │           │  Send "{Up/Down/ │
         │ AutoHotkey64   │ ─────────▶│   Enter}"        │
         └────────────────┘           └────────┬─────────┘
                                                 │ 注入到前台应用
                                                 ▼
                                       ┌──────────────────┐
                                       │  浏览器 / 应用    │
                                       │  收到 ↑/↓/Enter  │
                                       └──────────────────┘

数据流关键点：
1. scan code = 0 → Realtek 驱动注入，不是物理键盘
2. AHK v2 必须同时定义 Media_Play_Pause::（占位）+ vkB3::（生效）
3. 边沿检测：watcher 只在 PnP 状态变化时启停 AHK，不每轮询都重启